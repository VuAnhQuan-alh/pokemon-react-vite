const LayoutGame = () => {
  // lấy thông tin của danh sách pokemon

  // convert list pokemon to card component

  // render list pokemon
  // arrs: { name: string, link: string }[]

  // action click của user
  // state: listChoosePokemon, listCheckedTruePokemon
  // user click: => lưu name pokemon vào listChoosePokemon

  // bắt sự kiện listChoosePokemon.length === 2
  // th1 listChoosePokemon[0] === listChoosePokemon[1]
  // 1: luwu name vaof listCheckedTruePokemon
  // 2: cộng điểm cho user vào context game
  // 3: set listChoosePokemon = []
  // th2: false => set listChoosePokemon = []

  // render mang pokemon với props:
  // name, link, isChecked === listChoosePokemon.include(name), isDone === listCheckedTruePokemon

  // bắt sự kiện kết thúc game khi kết thúc thời gian trong context game
  return <></>;
};
export default LayoutGame;
