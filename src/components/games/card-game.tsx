const CardGame = () => {
  // props: id <=> name pokemon, isChecked: check trạng thái up/lật, isDone: check trạng thái user chọn đúng
  // link: link ảnh pokemon

  // css pokemon card

  // bắt sự kiện isChecked để lật hoặc úp thẻ

  // bắt sự kiện user chọn đúng thẻ
  // => ẩn thẻ
  return <></>;
};
export default CardGame;
